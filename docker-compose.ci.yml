# docker-compose.ci.yml — CI override: removes restart policy (masks crashes in CI)
# Usage: docker compose -f docker-compose.yml -f docker-compose.ci.yml up -d
services:
  backend:
    restart: "no"

  frontend:
    restart: "no"
